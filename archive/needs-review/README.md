# Needs-review archive

This directory preserves uploaded handover files whose filenames did not match
their contents or whose purpose could not be restored confidently.

Nothing here has been deleted. Review these files before deciding whether they
should become production assets, source documents, or be removed in a later
cleanup.

## Files

- `package-json-uploaded.jpg` - uploaded as `package.json`, actually a JPEG.
- `tailwind-config-uploaded.pdf` - uploaded as `tailwind.config.ts`, actually a PDF.
- `tsconfig-uploaded.pdf` - uploaded as `tsconfig.json`, actually a PDF.
- `uploaded-env-example-with-values.txt` - uploaded as `lib-payments-tabby.ts`,
  actually an environment-variable example containing concrete public IDs/test
  values. A sanitized `.env.example` was created at the repository root.
